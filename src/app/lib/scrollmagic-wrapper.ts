// @ts-ignore
import ScrollMagicRaw from 'scrollmagic';

// Create a default export if needed
const ScrollMagic = (ScrollMagicRaw?.ScrollMagic || ScrollMagicRaw?.default || ScrollMagicRaw);

export default ScrollMagic;